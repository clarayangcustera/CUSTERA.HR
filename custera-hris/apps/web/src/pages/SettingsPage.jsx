import React, { useEffect, useState } from 'react';
import { api } from '../api';
import { useAuth } from '../auth';
import Modal from '../components/Modal';
import EmptyState from '../components/EmptyState';

export default function SettingsPage() {
  const { user } = useAuth(); const [organization, setOrganization] = useState(null); const [departments, setDepartments] = useState([]); const [modal, setModal] = useState(false); const [form, setForm] = useState({ name: '', code: '' }); const [error, setError] = useState(''); const [saving, setSaving] = useState(false);
  const [passwordForm, setPasswordForm] = useState({ current_password: '', new_password: '', confirm_password: '' }); const [passwordMessage, setPasswordMessage] = useState(''); const [passwordError, setPasswordError] = useState(''); const [changingPassword, setChangingPassword] = useState(false);
  const isAdmin = user.role === 'ADMIN';
  async function load() { try { const [organizationData, departmentData] = await Promise.all([api('/api/settings'), api('/api/departments')]); setOrganization(organizationData); setDepartments(departmentData); } catch (err) { setError(err.message); } }
  useEffect(() => { load(); }, []);
  async function addDepartment(event) { event.preventDefault(); setSaving(true); try { await api('/api/departments', { method: 'POST', body: JSON.stringify(form) }); setModal(false); setForm({ name: '', code: '' }); await load(); } catch (err) { setError(err.message); } finally { setSaving(false); } }
  async function changePassword(event) { event.preventDefault(); setPasswordError(''); setPasswordMessage(''); if (passwordForm.new_password !== passwordForm.confirm_password) { setPasswordError('New password and confirmation do not match.'); return; } setChangingPassword(true); try { await api('/api/auth/change-password', { method: 'POST', body: JSON.stringify(passwordForm) }); setPasswordForm({ current_password: '', new_password: '', confirm_password: '' }); setPasswordMessage('Password updated successfully.'); } catch (err) { setPasswordError(err.message); } finally { setChangingPassword(false); } }
  return <div className="page"><div className="page-heading action-heading"><div><span className="eyebrow">ORGANISATION SETTINGS</span><h1>Organisation structure</h1><p>Reference settings used across employee placement, workflows and reports.</p></div>{isAdmin && <button className="button primary" onClick={() => { setError(''); setModal(true); }}>+ Add department</button>}</div>{error && <div className="alert alert-danger">{error}</div>}<div className="settings-grid"><section className="content-card"><div className="card-heading"><div><h2>Organisation profile</h2><p>Core settings for the currently selected workspace.</p></div></div>{organization ? <dl className="definition-list"><div><dt>Organisation</dt><dd>{organization.name}</dd></div><div><dt>Code</dt><dd>{organization.code}</dd></div><div><dt>Time zone</dt><dd>{organization.timezone}</dd></div><div><dt>Currency</dt><dd>{organization.currency}</dd></div></dl> : <div className="loading-block">Loading organisation settings…</div>}</section><section className="content-card"><div className="card-heading"><div><h2>Role policy</h2><p>Current access control in this production starter.</p></div></div><div className="role-list"><div><b>Administrator</b><span>Full organisation access including employees, configuration, payroll publishing and audit logs.</span></div><div><b>Manager</b><span>Employee records, leave/claim approvals, attendance and documents. No payroll publishing or audit log restriction bypass.</span></div><div><b>Employee</b><span>Self-service access to personal leave, claims, attendance, payslips and shared documents.</span></div></div></section></div><section className="content-card"><div className="card-heading"><div><h2>Account security</h2><p>Change the initial demo password before sharing this system with anyone.</p></div></div><form className="form-stack narrow-form" onSubmit={changePassword}><div className="form-grid three"><label>Current password<input required type="password" value={passwordForm.current_password} onChange={(event) => setPasswordForm({ ...passwordForm, current_password: event.target.value })} /></label><label>New password<input required minLength="10" type="password" value={passwordForm.new_password} onChange={(event) => setPasswordForm({ ...passwordForm, new_password: event.target.value })} /></label><label>Confirm new password<input required minLength="10" type="password" value={passwordForm.confirm_password} onChange={(event) => setPasswordForm({ ...passwordForm, confirm_password: event.target.value })} /></label></div>{passwordError && <div className="alert alert-danger">{passwordError}</div>}{passwordMessage && <div className="alert alert-info">{passwordMessage}</div>}<div><button className="button primary" disabled={changingPassword}>{changingPassword ? 'Updating…' : 'Update password'}</button></div></form></section><section className="content-card"><div className="card-heading"><div><h2>Departments</h2><p>Active departments available when creating or editing employee records.</p></div></div>{departments.length === 0 ? <EmptyState title="No departments" /> : <div className="department-list">{departments.map((department) => <div key={department.id}><span className="department-icon">⌁</span><div><b>{department.name}</b><small>{department.code || 'No department code'}</small></div><span className={department.active ? 'active-dot' : 'inactive-dot'}>{department.active ? 'Active' : 'Inactive'}</span></div>)}</div>}</section>{modal && <Modal title="Add department" onClose={() => setModal(false)}><form className="form-stack" onSubmit={addDepartment}><label>Department name<input required value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} /></label><label>Department code<input value={form.code} onChange={(event) => setForm({ ...form, code: event.target.value })} placeholder="HR" /></label>{error && <div className="alert alert-danger">{error}</div>}<div className="form-actions"><button className="button ghost" type="button" onClick={() => setModal(false)}>Cancel</button><button className="button primary" disabled={saving}>{saving ? 'Saving…' : 'Save department'}</button></div></form></Modal>}</div>;
}
